package com.example.chonkchat.data;

/**
 * Enum defines the type of message being sent.
 * 
 * @author Joseph Adamson
 */
public enum MessageType {
    CONNECTED, DISCONNECTED, TEXT, FILE, SERVER, SHUTDOWN
}
